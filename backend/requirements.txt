# ── 웹 서버 ──────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2

# ── 인증 ────────────────────────────────────────────────
pyjwt>=2.8
passlib>=1.7
bcrypt>=4.0,<4.1     # passlib 1.7.x 와의 호환을 위해 상한 고정

# ── LangChain / RAG ─────────────────────────────────────
langchain>=0.2
langchain-core>=0.2
langchain-community>=0.2
langchain-chroma>=0.1
langchain-huggingface>=0.0.3      # 임베딩
langchain-ollama>=0.1             # 로컬 LLM/임베딩
langchain-openai>=0.1           # OpenAI로 교체 시 주석 해제
langchain-anthropic>=0.1        # Anthropic으로 교체 시 주석 해제
sentence-transformers>=2.6        # HF 임베딩 & rerank cross-encoder

# ── Graph DB ────────────────────────────────────────────
neo4j>=5.18
python-dotenv>=1.0
